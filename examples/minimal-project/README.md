# Minimal project

This synthetic example contains one six-second scene and no private source material. Use the repository initializer with `--example` to copy it into a runnable project.

The example demonstrates method and file structure only. Full render acceptance is performed separately after installing dependencies.
